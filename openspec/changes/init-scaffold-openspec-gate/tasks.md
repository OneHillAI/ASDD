## 1. Scaffold the openspec gate
- [ ] 1.1 Add `openspec-gate.py` and `_openspec_locate.py` to the `--goose` copy loop in `cli/init.sh`
- [ ] 1.2 Assert in `cli/init.test.sh` that both files land in the scaffolded repo
